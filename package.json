{
  "name": "loginbdd",
  "version": "1.0.0",
  "description": "proyecto de login con bdd",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:runChrome": "cypress run --browser chrome --spec cypress/e2e/features/login.feature"

  },
  "author": "fjja",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.1.0",
    "cypress": "^15.2.0"
  },
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.6",
    "@cypress/grep": "^5.0.0",
    "cypress-mochawesome-reporter": "^4.0.2"
  }
}
